{
  "name": "bamazon.js",
  "version": "0.0.1",
  "description": "app using node to return results from a database and allows user to enter information getting an answer returned to the console.",
  "main": "bamazonCustomer.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "me",
  "license": "ISC",
  "dependencies": {
    "inquirer": "^3.0.6",
    "mysql": "^2.13.0"
  }
}
